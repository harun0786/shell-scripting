
the user gives commands to the linux system
shell/bash
the shell bash reads the userb s command and sends it to the kernel
kernel
the kernel manages system resources and communicates with the the hardware
hardware
the hardware performs the actual pyhsical opertions requested by the kernel
question ?
q when a user types a command which layer reads the command first
the shell/bash layer reads the command first

what does chmod u+x hello.sh do?
chmod u+x hello.sh gives the execute x permission to the owner u of hello.sh 

why can bash hello.sh run a readable script without its execute bit?
because bash reads the script file and executes it directly
why does ./hello.sh require execute permission 
because linux executes the file directly so it must have execute permission
what does ./ mean?
./ means the current directory

